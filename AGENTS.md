# Agents

## Context

- This is a NixOS configuration for a homelab
- The local IP of the homelab is `192.168.1.67`
- You have SSH access to the homelab via the local IP

## Build and Deployment Commands

- `just build`: build the current configuration
- `just deploy`: deploy the current configuration
